import React from 'react';

/**
 * Componente removido conforme solicitação do usuário.
 * Retorna null para não interferir na interface e não causar erros de carregamento.
 */
export const Chat: React.FC = () => {
  return null;
};